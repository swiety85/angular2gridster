/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {GridList} from './grid-list';

describe('GridList', () => {
  it('should create an instance', () => {
    expect(new GridList()).toBeTruthy();
  });
});
