/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Gridster} from './gridster';

describe('Gridster', () => {
  it('should create an instance', () => {
    expect(new Gridster()).toBeTruthy();
  });
});
